@model KooperatifYonetim.Core.Entities.YemTuru
@{
    ViewData["Title"] = "Yem Türü Ekle";
}

<div class="d-flex align-items-center mb-4">
    <a asp-action="Index" class="btn btn-sm btn-outline-secondary me-3">
        <i class="bi bi-arrow-left"></i> Geri
    </a>
    <h4 class="fw-bold mb-0">Yeni Yem Türü</h4>
</div>

<div class="row justify-content-center">
    <div class="col-lg-5">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()

                    <div class="mb-3">
                        <label asp-for="Ad" class="form-label fw-semibold">Ad</label>
                        <input asp-for="Ad" class="form-control" placeholder="Örn: Ot" />
                        <span asp-validation-for="Ad" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Birim" class="form-label fw-semibold">Birim</label>
                        <select asp-for="Birim" asp-items="Html.GetEnumSelectList<KooperatifYonetim.Core.Enums.YemBirim>()" class="form-select"></select>
                    </div>

                    <div class="mb-4 form-check">
                        <input asp-for="AktifMi" class="form-check-input" type="checkbox" />
                        <label asp-for="AktifMi" class="form-check-label">Aktif</label>
                    </div>

                    <div class="d-flex gap-2">
                        <a asp-action="Index" class="btn btn-outline-secondary flex-fill">İptal</a>
                        <button type="submit" class="btn btn-primary flex-fill">
                            <i class="bi bi-save me-1"></i> Kaydet
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
