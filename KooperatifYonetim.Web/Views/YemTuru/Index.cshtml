@model IEnumerable<KooperatifYonetim.Core.Entities.YemTuru>
@{
    ViewData["Title"] = "Yem Türleri";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0">Yem Türleri</h4>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i> Yeni Yem Türü
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">@TempData["Success"]<button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
}

<div class="card border-0 shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th>Ad</th>
                        <th>Birim</th>
                        <th>Durum</th>
                        <th class="text-end">İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var yt in Model)
                    {
                        <tr class="@(yt.AktifMi ? "" : "table-secondary text-muted")">
                            <td class="fw-semibold">@yt.Ad</td>
                            <td>
                                @if (yt.Birim == KooperatifYonetim.Core.Enums.YemBirim.Balya)
                                {
                                    <span class="badge bg-info text-dark">Balya</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Çuval</span>
                                }
                            </td>
                            <td>
                                @if (yt.AktifMi)
                                {
                                    <span class="badge bg-success">Aktif</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Pasif</span>
                                }
                            </td>
                            <td class="text-end">
                                <a asp-action="Edit" asp-route-id="@yt.YemTuruId" class="btn btn-sm btn-outline-primary me-1">
                                    <i class="bi bi-pencil"></i>
                                </a>
                                <form asp-action="ToggleAktif" asp-route-id="@yt.YemTuruId" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn btn-sm @(yt.AktifMi ? "btn-outline-warning" : "btn-outline-success")"
                                            title="@(yt.AktifMi ? "Pasife Al" : "Aktife Al")">
                                        <i class="bi @(yt.AktifMi ? "bi-pause-circle" : "bi-play-circle")"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
